[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Robot Air Hockey Challenge"
authors = [{ name = "Atalay Donat", email = "atalay.donat@robot-learning.de" }]
dynamic = ["version", "description"]
